// go, rest, pipe, reduce, each, keys, isObject;
// filter, each, keys, isObject;
// map, each, keys, isObject + get, curryr;

// go, rest, pipe, reduce, each, keys, isObject, filter, map, get, curryr;
// a1, a2, a3, a4, a5, a6, a7, a8, a9, a10, a11;
// functions = 11;

// users;
// a0;

goF(a0, a8, a9, a10);

function goF(argument) {
  const functions = arguments;
  return a3.apply(null, functions)(argument);
}

function a3() {
  const functions = arguments;

  return functions(argument) {
    return a4(functions, function(argument, fn) {
      return fn(argument);
    }, argument)
  }
}